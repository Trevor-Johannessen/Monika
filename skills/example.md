# Example Skill

This is an example skill file. Replace or add `.md` files in the `skills/` directory to teach the agent new behaviors or knowledge.

Each file is loaded at startup and included in the system context.
